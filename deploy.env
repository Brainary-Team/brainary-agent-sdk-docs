# 部署基路径（全站唯一开关，snapshot.sh 与 build.sh 共同读取）。
#
# GitHub Pages「项目页」挂在子路径 → 用仓库名前缀：
#   SITE_BASE=/brainary-agent-sdk-docs/  →  https://zibuyu2015831.github.io/brainary-agent-sdk-docs/
#
# 切换到「自定义域名 / 根部署」时改为：
#   SITE_BASE=/
# 并：① 在仓库根放一个 CNAME 文件（内容为域名，build.sh 会拷进 dist）；
#     ② 重新冻结当前版本（旧冻结把子路径烤死了，见 README）；
#     ③ 在 GitHub 配置自定义域名 + DNS。
#
# 规则：首尾必须带 '/'。环境变量 SITE_BASE 可临时覆盖本文件。
SITE_BASE=/
