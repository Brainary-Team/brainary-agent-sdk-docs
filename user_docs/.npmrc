# Mermaid（经 vitepress-plugin-mermaid 的 withMermaid）会把一批传递依赖塞进 Vite 的
# optimizeDeps.include（mermaid、cytoscape、dayjs、@braintree/sanitize-url、debug…）。
# pnpm 默认隔离布局下这些裸包名在项目根不可解析 → dev 下 Mermaid 预构建失败 → 页面空白。
# Mermaid 依赖树很深、需要的名字不固定，故直接把依赖公开提升到顶层 node_modules（等效 shamefully-hoist）。
# 这是纯文档站，node_modules 扁平化无副作用。
public-hoist-pattern[]=*
